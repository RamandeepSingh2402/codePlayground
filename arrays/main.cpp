#include <iostream>

using namespace std;

int main() {
    int arr[] = {2,3,4,5,6,6};
    // printing the numbers inside an array
    cout<<arr[0]<<endl;

    // resetting the array's elements
    arr[0] = 20;

    // printing the updated element in the array
    cout<< arr[0] << endl;

    cout << "Hello, World!" << endl;
    return 0;
}